import React, { FC, memo } from 'react';
import './Calendar.scss';

export const Calendar: FC = memo(() => {
  return (
    <div className="calendar">
      <h4>calendar CONTENT</h4>
    </div>
  );
});
