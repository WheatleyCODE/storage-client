import React, { FC, memo } from 'react';
import './StorageLastItem.scss';

export const StorageLastItem: FC = memo(() => {
  return (
    <div className="storage-last-item">
      <div>StorageLastItem</div>
    </div>
  );
});
